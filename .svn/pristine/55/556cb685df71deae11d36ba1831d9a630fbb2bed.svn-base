<?php
	/**
	 *	实现权限的验证
	 *
	 */
	class ManageAuth {
		private $CI;
		public function __construct() 
		{
			$this->CI = &get_instance();     
		}  
		/**
		 * 权限认证     
		 */    
		public function auth() {

			//在这里获取到用户所允许范围的操作
			/*
			$res= getcookie_crm('islogin');
			
			$this->CI->load->model('employee_model');
			
			$data= $this->CI->employee_model->getEmployeeAllInfo();
			print_r($data);
			die;
			*/
			//var_dump($res);
			//die;

			/*
			
			//检测如果是 admin 而且第二个参数不是login的话，检测是否有进入后台的权限。
			//如果有,进入后台首页,没有就跳到 admin/login里面。
			
			if ($admin==='admin' && $login!=='login') {
				// 需要进行权限检查的URL
				$this->CI->load->library('session');
				if( !$this->CI->session->userdata('username') ) {
					// 用户未登陆               
					redirect($admin.'/login'); 
					return;            
				}        
			} 
			*/           
		}        
	}
